<% if flash[:notice] %>
 <p><%= flash[:notice] %></p>
<% end %>

  <title>WebCampBackendNew</title><meta name="csrf-param" content="authenticity_token"><meta name="csrf-token" content="mWumL0jsD1tl5O3BGhq8/CvzARAqWKThb40w/fEb4V2IHWpcLCr3tOhfEYQINjH8SFJ3kYrF/o3+gBukLFyDNw=="></head>

  <body>

<h1>Books</h1>

<table>
  <thead>
    <tr>
      <th>本のタイトル</th>
      <th>感想</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
    <tr>

        <td><%= book.title %></td>
        <td><%= book.body %></td>
        <td><%= link_to 'Show', book_path(book) %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book_path(book), method: :delete %></td>
        <% end %>

    </tr>
  </tbody>
</table>


<h2>New book</h2>

<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <% @book.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>

<%= form_with model: @book, url:'/books',local:true do |f| %>

 <h3>本のタイトル</h3>
 <%= f.text_field :title %>

 <h3>感想</h3>
 <%= f.text_area :body %>
 <br><%= f.submit 'Create Book' %></br>

<% end %>



</body>